using System;
// 숫자나 텍스트와 같은 데이터를 다룰 수 있는 기본적인 데이터 처리 클래스 인 System을 사용하겠다.
// 다시말해 System 이라는 namespace 안에있는 클래스를 사용하겠다고 컴파일러에게 선언
namespace BrainCSharp {
    // namespace : 성격이나 하는 일이 비슷한 클래스, 구조체, 인터페이스, 델리게이트, 열거 형식 등을 하나의 이름 아래 묶는 일을 함

    /* 만약 다른 namespace에서 HelloWorld 클래스를 사용하고자 할때는 using BrainCSharp; 문장을 이용하여 
       BrainCSharp namespace를 참조하거나 BrainCSharp.HelloWorld 처럼 클래스가 소속되어 있는 namespace 와 class 의 이름을 붙여줘야 한다.  
    */
    class HelloWorld {
        static void Main(string[] args) { // 프로그램 실행이 시작되는 곳
            Console.WriteLine("Hello, World!!"); // Hello, World!! 를 프롬프트에 출력
            Console.WriteLine("Hello, {0}!", args[0]); // 에러가 뜰 경우   1차적으로 프로젝트 - (프로젝트이름) 속성 -  디버그 - 명령줄 인수에 적기
        }
    }
} 
// 시작 후 자동종료를 원치 않을 때는 Ctrl + F5 로 실행

// 명령줄 인수를 적지 않은 상태에서는 콘솔창을 켜고 HelloWorld.exe 가 있는 HelloWorld 폴더 하위 HelloWorld.exe 가 있는 폴더까지 접근 후 HelloWorld HG 라고 치면 인수를 가지고 프로그램이 실행된다.
// HG 라고 적은 것이 arge[0] 이다.

/*
Note
- CLR : 자바의 가상머신과 비슷한 역할을 수행.
- CLR 은 단순히 C#이나 기타 언어들을 동작시키는 환경 기능뿐만 아니라, 프로그램에 예외가 발생했을 때 이를 처리하도록 도와주는 기능, 언어 간의 상속 지원, COM과 상호 운영성 지원, 가비지 컬렉션 기능을 포함
 */
